@extends('layouts.admin') @section('title', 'Detail Pelanggan') @section('content')
<a href="{{ route('admin.customers') }}" class="text-sm text-orange-500 mb-4 inline-block">← Kembali</a>
<div class="bg-white rounded-xl p-6 shadow-sm border mb-6"><h3 class="font-bold text-lg mb-2">{{ $user->name }}</h3><p class="text-sm text-gray-500">{{ $user->email }} · {{ $user->phone ?? '-' }}</p><p class="text-sm text-gray-500">Bergabung: {{ $user->created_at->format('d M Y') }}</p></div>
<h3 class="font-bold mb-4">Riwayat Pesanan</h3>
<div class="bg-white rounded-xl shadow-sm border overflow-hidden"><table class="w-full text-sm"><thead class="bg-gray-50 text-xs uppercase"><tr><th class="px-6 py-3 text-left">Order</th><th class="px-6 py-3">Layanan</th><th class="px-6 py-3">Total</th><th class="px-6 py-3">Status</th><th class="px-6 py-3">Tanggal</th></tr></thead><tbody class="divide-y">@foreach($orders as $o)<tr><td class="px-6 py-4"><a href="{{ route('admin.orders.show', $o) }}" class="text-orange-500 font-medium">{{ $o->order_number }}</a></td><td class="px-6 py-4 text-center">{{ $o->cateringService->name ?? '-' }}</td><td class="px-6 py-4 text-center">Rp {{ number_format($o->total,0,',','.') }}</td><td class="px-6 py-4 text-center"><span class="px-2 py-1 rounded-full text-xs font-medium {{ match($o->status) { 'completed'=>'bg-green-100 text-green-700','cancelled'=>'bg-red-100 text-red-700',default=>'bg-gray-100 text-gray-700' } }}">{{ $o->status_label }}</span></td><td class="px-6 py-4 text-center">{{ $o->created_at->format('d/m/Y') }}</td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $orders->links() }}</div>@endsection
